# BitWet Environment Variables
# Copy this file to .env.local for local development

# Set to 'true' only for Capacitor iOS static export builds (used by build:ios script)
# Do NOT set this for Vercel deployment — Vercel uses server-side rendering
# NEXT_PUBLIC_STATIC_EXPORT=true
